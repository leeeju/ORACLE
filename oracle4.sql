DROP TABLE TEST;

CREATE TABLE TEST(
A NUMBER(5) PRIMARY KEY,
B VARCHAR(10)
);

CREATE SEQUENCE EMP_SEQ
	START WITH 1
	INCREMENT BY 1
	MAXVALUE 99999
	NOCYCLE
	NOCACHE;
    
INSERT INTO TEST VALUES(EMP_SEQ.NEXTVAL, 'AAA');

SELECT
    *
FROM TEST;

ROLLBACK;

COMMENT;

SELECT * FROM USER_SEQUENCES;

SELECT *
FROM TEST;


TRUNCATE TABLE TEST;

INSERT INTO TEST VALUES( (SELECT COUNT(*) +1 FROM TEST), 'AAA');

COMMIT;
INSERT INTO TEST VALUES( (SELECT COUNT(*) +1 FROM TEST), 'AAA');
COMMIT;
ROLLBACK;

SELECT *
FROM TEST;


drop TABLE emp1;

CREATE TABLE EMP1
AS
SELECT * FROM EMP;

CREATE INDEX EMP_IDX ON EMP (EMPLOYEE_ID);


SELECT * 
FROM EMP
WHERE EMPLOYEE_ID = 105;

SELECT * 
FROM EMP1
WHERE EMPLOYEE_ID = 105;


CREATE INDEX EMP_SALARY_IDX
	ON EMP(SALARY * 12) ;
    
SELECT *
FROM USER_IND_COLUMNS ;

CREATE SYNONYM E FOR EMP;

SELECT * FROM E;

SELECT department_id, TO_NUMBER (null) location, hire_date
FROM emp
UNION
SELECT department_id, location_id, TO_DATE (null)
FROM dept ;


CREATE TABLE TT5
AS
SELECT EMPLOYEE_ID , JOB_ID 
FROM EMP
UNION                  
SELECT EMPLOYEE_ID, JOB_ID
FROM JOB_HISTORY ;


